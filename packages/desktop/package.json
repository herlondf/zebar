{
  "name": "@zebar/desktop",
  "version": "0.0.0",
  "scripts": {
    "build": "pnpm --filter zebar run build && pnpm tauri build -- --verbose",
    "dev": "pnpm tauri dev",
    "dev:local": "pnpm dev",
    "format": "cargo +nightly fmt",
    "lint": "cargo +nightly fmt --check && cargo clippy --all-targets --workspace -- -D warnings",
    "tauri": "tauri"
  },
  "dependencies": {},
  "devDependencies": {
    "@tauri-apps/cli": "2.0.3",
    "typescript": "5.3.3"
  }
}
